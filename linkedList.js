class Node {
	constructor(value) {
		this.value = value;
		this.nextNode = null;
	}
}
export class LinkedList {
	constructor(node) {
		this.node = node;
		this.value = node.value;
	}

	append = (value) => {};
}
